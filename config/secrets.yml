development:
  secret_key_base: my_secret_key

test:
  secret_key_base: my_secret_key

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: my_secret_key_that_should_prolly_be_hidden